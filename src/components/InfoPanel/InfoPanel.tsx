import React from 'react';

type Props = {};

const InfoPanel = (props: Props) => {
  return (
    <div>
      Facebook
      <span>{'>'} </span>
      React
    </div>
  );
};

export default InfoPanel;
